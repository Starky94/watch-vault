services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-watchvault}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-watchvault}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    volumes:
      - postgres-data:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      PORT: ${PORT:-3001}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3001/api/health').then((response) => { if (!response.ok) process.exit(1) }).catch(() => process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${WEB_PORT:-8080}:80"

  importer:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_COMMAND:-npm run import:movies}
      ADMIN_JOB_KEY: popular
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-tv-popular:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_TV_POPULAR_COMMAND:-npm run import:tv-popular}
      ADMIN_JOB_KEY: tv-popular
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-games:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_GAMES_COMMAND:-npm run import:games}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_GAMES_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: games-popular
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-games-recently-released:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_GAMES_RECENTLY_RELEASED_COMMAND:-npm run import:games-recently-released}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_GAMES_RECENTLY_RELEASED_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: games-recently-released
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-games-upcoming:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_GAMES_UPCOMING_COMMAND:-npm run import:games-upcoming}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_GAMES_UPCOMING_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: games-upcoming
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-books:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_BOOKS_COMMAND:-npm run import:books}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_BOOKS_INTERVAL_SECONDS:-3600}
      ADMIN_JOB_KEY: books
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-now-playing:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_NOW_PLAYING_COMMAND:-npm run import:now-playing}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_NOW_PLAYING_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: now-playing
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-tv-airing-today:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_TV_AIRING_TODAY_COMMAND:-npm run import:tv-airing-today}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_TV_AIRING_TODAY_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: tv-airing-today
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-upcoming:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_UPCOMING_COMMAND:-npm run import:upcoming}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_UPCOMING_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: upcoming
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  importer-tv-on-the-air:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${IMPORT_TV_ON_THE_AIR_COMMAND:-npm run import:tv-on-the-air}
      IMPORT_INTERVAL_SECONDS: ${IMPORT_TV_ON_THE_AIR_INTERVAL_SECONDS:-86400}
      ADMIN_JOB_KEY: tv-on-the-air
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  alert-dispatcher:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["./docker/api/importer-loop.sh"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      IMPORT_COMMAND: ${ALERT_DISPATCH_COMMAND:-npm run dispatch:alerts}
      IMPORT_INTERVAL_SECONDS: ${ALERT_DISPATCH_INTERVAL_SECONDS:-900}
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

  theme-scheduler:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    command: ["npm", "run", "schedule:themes"]
    env_file:
      - ${APP_ENV_FILE:-.env}
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/watchvault}
      THEME_SCHEDULER_TIME_ZONE: ${THEME_SCHEDULER_TIME_ZONE:-Europe/Bucharest}
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_healthy

volumes:
  postgres-data:
